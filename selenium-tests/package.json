{
  "name": "vastra-selenium-tests",
  "version": "1.0.0",
  "description": "Enterprise Selenium E2E test suite for Vastra Web App",
  "main": "conftest.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js --require ./conftest.js --timeout 60000 --exit"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "selenium-webdriver": "^4.18.0"
  },
  "devDependencies": {
    "mocha": "^10.3.0"
  }
}
